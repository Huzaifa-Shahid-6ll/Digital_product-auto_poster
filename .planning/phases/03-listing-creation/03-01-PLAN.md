---
phase: 03-listing-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [MK-01]
user_setup:
  - service: etsy
    why: "Etsy OAuth requires API keys from Etsy Developer Portal"
    env_vars:
      - name: ETSY_API_KEY
        source: "Etsy Developer Portal -> Apps -> Your App"
      - name: ETSY_API_SECRET
        source: "Etsy Developer Portal -> Apps -> Your App"
      - name: ETSY_OAUTH_REDIRECT_URI
        source: "Your application's callback URL (must match Etsy app config)"

must_haves:
  truths:
    - "User can initiate Etsy OAuth flow via API"
    - "User can complete OAuth and system stores refresh token"
    - "System can auto-refresh expired access tokens"
    - "User can list and select from multiple Etsy shops"
  artifacts:
    - path: "src/etsy/__init__.py"
      provides: "Etsy module package init"
    - path: "src/etsy/oauth.py"
      provides: "OAuth flow implementation with PKCE"
      exports: ["EtsyOAuth", "generate_pkce_pair", "build_auth_url"]
    - path: "src/etsy/client.py"
      provides: "Etsy API client with token auto-refresh"
      exports: ["EtsyClient"]
    - path: "src/api/etsy_routes.py"
      provides: "OAuth API endpoints"
      exports: ["/api/auth/etsy/authorize", "/api/auth/etsy/callback", "/api/auth/etsy/shops"]
  key_links:
    - from: "src/api/etsy_routes.py"
      to: "src/etsy/oauth.py"
      via: "import EtsyOAuth"
    - from: "src/etsy/client.py"
      to: "src/api/main.py"
      via: "dependency injection pattern"
---

<objective>
Implement Etsy OAuth flow with token management and multi-shop support.

Purpose: Establish secure authentication with Etsy enabling all subsequent listing operations. OAuth is the foundation - no other Etsy operations work without it.

Output: Working OAuth flow with token storage and auto-refresh.
</objective>

<context>
@.planning/phases/03-listing-creation/03-RESEARCH.md (OAuth patterns, token refresh)
@.planning/phases/03-listing-creation/03-CONTEXT.md (D-01, D-02 locked decisions)
@.planning/phases/03-listing-creation/03-UI-SPEC.md (API endpoints)

# Reference existing patterns
@src/api/idea_routes.py (route pattern)
@src/api/main.py (FastAPI app setup)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Etsy module structure</name>
  <files>src/etsy/__init__.py</files>
  <action>
Create the etsy module package with basic structure:

```python
"""Etsy API integration module."""

from .oauth import EtsyOAuth, generate_pkce_pair, build_auth_url
from .client import EtsyClient

__all__ = ["EtsyOAuth", "generate_pkce_pair", "build_auth_url", "EtsyClient"]
```

This establishes the module structure that subsequent tasks will populate.
  </action>
  <verify>
<automated>python -c "from src.etsy import EtsyClient; print('OK')"</automated>
  </verify>
  <done>Etsy module imports without errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement Etsy OAuth flow with PKCE</name>
  <files>src/etsy/oauth.py</files>
  <action>
Create OAuth implementation following D-01 (full OAuth with refresh token) and D-02 (multi-shop support):

- `generate_pkce_pair()`: Generate code_verifier and code_challenge for PKCE
- `build_auth_url()`: Construct Etsy authorization URL with scopes
- `EtsyOAuth` class:
  - Store client_id, client_secret, redirect_uri
  - Generate and store PKCE verifier (encrypted)
  - Build authorization URL
  - Exchange code for tokens (access_token, refresh_token, expires_in)
  - Auto-refresh access token before expiration
  - Store encrypted refresh_token in database
  - Support multiple shops (shop_id association)

Reference: 03-RESEARCH.md Pattern 1 for token refresh logic.

Per D-04 from Phase 1: Implement 3 retries with exponential backoff.
  </action>
  <verify>
<automated>python -c "from src.etsy import EtsyOAuth; o = EtsyOAuth('test', 'secret', 'http://localhost'); print(hasattr(o, 'refresh_token'))"</automated>
  </verify>
  <done>OAuth class has token refresh capability, stores refresh_token</done>
</task>

<task type="auto">
  <name>Task 3: Implement Etsy API client with auto-refresh</name>
  <files>src/etsy/client.py</files>
  <action>
Create EtsyClient wrapper with auto-refresh:

- Base URL: https://api.etsy.com/v3/application
- Headers: x-api-key (client_id:client_secret format), Authorization (Bearer token)
- `get_valid_token()`: Check expiry, refresh if needed
- Methods: `create_draft_listing()`, `get_listing()`, `update_listing()`, `upload_image()`, `upload_file()`, `get_shops()`
- All API calls use `get_valid_token()` before making request
- Per D-04 from Phase 1: 3 retries with exponential backoff on failures

Reference: 03-RESEARCH.md Pattern 1 and Code Examples sections.
  </action>
  <verify>
<automated>python -c "from src.etsy import EtsyClient; print(hasattr(EtsyClient, 'create_draft_listing'))"</automated>
  </verify>
  <done>Client has all required Etsy API methods with token auto-refresh</done>
</task>

<task type="auto">
  <name>Task 4: Create OAuth API routes</name>
  <files>src/api/etsy_routes.py</files>
  <action>
Create FastAPI routes following existing patterns (per 03-UI-SPEC.md):

- `GET /api/auth/etsy/authorize`: Initiate OAuth, return auth URL for frontend redirect
- `GET /api/auth/etsy/callback?code=XXX&state=XXX`: Handle OAuth callback, exchange code for tokens, store in DB
- `GET /api/auth/etsy/shops`: List user's Etsy shops (requires auth)
- `DELETE /api/auth/etsy/disconnect`: Revoke tokens, disconnect shop

Request models (Pydantic):
- `EtsyAuthorizeResponse`: {authorization_url, state, shop_id?}

Response models:
- `EtsyShop`: {shop_id, shop_name, is_active}
- `EtsyCallbackResponse`: {success, shop_id, shop_name}

Mount in main.py: `app.include_router(etsy_router, prefix="/api/auth/etsy", tags=["etsy"])`
  </action>
  <verify>
<automated>python -c "from src.api.etsy_routes import router; print([r.path for r in router.routes])"</automated>
  </verify>
  <done>All OAuth endpoints registered and importable</done>
</task>

</tasks>

<verification>
- OAuth flow can be initiated via API
- Token refresh happens automatically before expiry
- Multiple shops can be connected and listed
- All endpoints follow existing route patterns
</verification>

<success_criteria>
OAuth flow completes successfully - user authorizes, system stores refresh token, can list shops. Token auto-refresh works without user intervention.
</success_criteria>

<output>
After completion, create `.planning/phases/03-listing-creation/03-01-SUMMARY.md`
</output>