---
phase: 03-listing-creation
plan: 04
type: execute
wave: 4
depends_on: [03-02]
files_modified: []
autonomous: true
requirements: [MK-02]
user_setup: []

must_haves:
  truths:
    - "Prohibited keywords are filtered from listing title/description/tags"
    - "AI disclosure text is added to listing description"
    - "Multiple listings are staggered to avoid spam detection"
    - "All three compliance features active by default"
  artifacts:
    - path: "src/compliance/__init__.py"
      provides: "Compliance module package init"
    - path: "src/compliance/keyword_filter.py"
      provides: "Prohibited keyword filtering"
      exports: ["filter_keywords", "PROHIBITED_KEYWORDS"]
    - path: "src/compliance/ai_disclosure.py"
      provides: "AI content disclosure text"
      exports: ["add_ai_disclosure", "AI_DISCLOSURE_TEXT"]
    - path: "src/compliance/stagger.py"
      provides: "Staggered publishing logic"
      exports: ["calculate_stagger_delay", "StaggerConfig"]
    - path: "src/compliance/apply.py"
      provides: "Combined compliance application"
      exports: ["apply_compliance"]
  key_links:
    - from: "src/api/listing_routes.py"
      to: "src/compliance/apply.py"
      via: "Call apply_compliance before creating listing"
---

<objective>
Implement compliance layer for Etsy listings.

Purpose: Per D-10, D-11, D-12, D-13 - filter prohibited keywords, add AI disclosure, stagger publishing. All three compliance features active by default.

Output: Compliance module applied to all listing creation.
</objective>

<context>
@.planning/phases/03-listing-creation/03-RESEARCH.md (Pattern 4 - Compliance Pre-Processing)
@.planning/phases/03-listing-creation/03-CONTEXT.md (D-10, D-11, D-12, D-13 locked decisions)
@.planning/phases/03-listing-creation/03-02-PLAN.md (Listing creation - compliance applies to listings)

# Reference
@src/api/listing_routes.py (integration point)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compliance module structure</name>
  <files>src/compliance/__init__.py</files>
  <action>
Create compliance module:

```python
"""Compliance layer for Etsy listings."""

from .keyword_filter import filter_keywords, PROHIBITED_KEYWORDS
from .ai_disclosure import add_ai_disclosure, AI_DISCLOSURE_TEXT
from .stagger import calculate_stagger_delay, StaggerConfig
from .apply import apply_compliance

__all__ = [
    "filter_keywords",
    "PROHIBITED_KEYWORDS",
    "add_ai_disclosure",
    "AI_DISCLOSURE_TEXT",
    "calculate_stagger_delay",
    "StaggerConfig",
    "apply_compliance",
]
```
  </action>
  <verify>
<automated>python -c "from src.compliance import apply_compliance; print('OK')"</automated>
  </verify>
  <done>Compliance module imports without errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement keyword filtering</name>
<files>src/compliance/keyword_filter.py</files>
  <action>
Implement prohibited keyword filter per D-10:

- `PROHIBITED_KEYWORDS`: List of terms to filter (for digital products):
  - "handmade", "hand-crafted", "handmade with"
  - "custom made", "made to order"
  - "one of a kind" (when overused)
  - Add more based on Etsy digital product guidelines

- `filter_keywords(title, description, tags) -> (title, description, tags)`:
  - Replace prohibited words with "[filtered]" or remove
  - Apply to title, description, and all tags
  - Case-insensitive matching
  - Preserve original structure/layout

Reference: 03-RESEARCH.md Pattern 4 (Compliance Pre-Processing).
  </action>
  <verify>
<automated>python -c "from src.compliance.keyword_filter import filter_keywords; result = filter_keywords('Handmade planner', 'A custom made guide', ['handmade', 'printable']); print(result[0])"</automated>
  </verify>
  <done>Keyword filter removes/replaces prohibited terms</done>
</task>

<task type="auto">
  <name>Task 3: Implement AI disclosure</name>
  <files>src/compliance/ai_disclosure.py</files>
  <action>
Implement AI disclosure text per D-11:

- `AI_DISCLOSURE_TEXT`: 
  ```python
  AI_DISCLOSURE = (
      "This listing was created using AI assistance. "
      "Please message me if you have any questions!"
  )
  ```

- `add_ai_disclosure(description) -> description`:
  - Append disclosure to description (or prepend if preferred)
  - Add separator (---) before disclosure
  - Per D-11: Add required disclosure text for AI-generated content

Reference: 03-RESEARCH.md Pattern 4.
  </verify>
  <verify>
<automated>python -c "from src.compliance.ai_disclosure import add_ai_disclosure; result = add_ai_disclosure('Product description'); print('AI' in result)"</automated>
  </verify>
  <done>AI disclosure text added to description</done>
</task>

<task type="auto">
  <name>Task 4: Implement staggered publishing</name>
  <files>src/compliance/stagger.py</files>
  <action>
Implement staggered publishing per D-12:

- `StaggerConfig`:
  ```python
  class StaggerConfig(BaseModel):
      delay_hours: int = 24  # Default 24 hours between listings
      max_listings_per_day: int = 5
  ```

- `calculate_stagger_delay(listing_index, config) -> timedelta`:
  - Calculate delay based on listing position in batch
  - Per D-12: Spread multiple listings over time to avoid spam detection
  - Default: 24 hours between listings
  - Configurable (user can adjust)

- Integration: Store last_published timestamp, calculate next available time

Reference: 03-RESEARCH.md Pattern 4.
  </action>
  <verify>
<automated>python -c "from src.compliance.stagger import calculate_stagger_delay; from datetime import timedelta; result = calculate_stagger_delay(2); print(result)"</automated>
  </verify>
  <done>Stagger delay calculation returns correct delay</done>
</task>

<task type="auto">
  <name>Task 5: Implement combined compliance application</name>
  <files>src/compliance/apply.py</files>
  <action>
Create combined compliance function:

- `apply_compliance(title, description, tags, stagger_index=0) -> ComplianceResult`:
  - Run keyword filter on title, description, tags
  - Add AI disclosure to description
  - Calculate stagger delay based on index
  - Return: {title, description, tags, stagger_delay}

- Per D-13: Full compliance - all three compliance features active by default
- Integration: Called in listing_routes.py before creating listing

Reference: 03-RESEARCH.md Pattern 4 (full example).
  </action>
  <verify>
<automated>python -c "from src.compliance.apply import apply_compliance; result = apply_compliance('Title', 'Description', ['tag1']); print(result.title, result.stagger_delay)"</automated>
  </verify>
  <done>Combined compliance applies all three features</done>
</task>

</tasks>

<verification>
- Keyword filter removes prohibited words from listing content
- AI disclosure appears in listing description
- Multiple listings are staggered appropriately
- All compliance features active by default
</verification>

<success_criteria>
All listing content passes through compliance layer - keywords filtered, AI disclosure added, publishing staggered. User can toggle compliance features if needed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-listing-creation/03-04-SUMMARY.md`
</output>