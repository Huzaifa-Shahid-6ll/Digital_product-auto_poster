---
phase: 03-listing-creation
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: []
autonomous: true
requirements: [MK-02]
user_setup: []

must_haves:
  truths:
    - "User can upload product images to Etsy listing"
    - "Images appear correctly in Etsy listing"
    - "Digital product files (PDF) upload to Etsy for file delivery"
    - "Etsy handles download delivery to buyers"
  artifacts:
    - path: "src/etsy/image_upload.py"
      provides: "Image upload to Etsy CDN"
      exports: ["upload_listing_image", "upload_product_images"]
    - path: "src/etsy/file_upload.py"
      provides: "Digital file upload for delivery"
      exports: ["upload_digital_file"]
    - path: "src/api/listing_routes.py"
      provides: "Add image/file upload endpoints to existing routes"
  key_links:
    - from: "src/api/listing_routes.py"
      to: "src/etsy/image_upload.py"
      via: "POST /api/listings/{id}/images"
    - from: "src/api/listing_routes.py"
      to: "src/etsy/file_upload.py"
      via: "POST /api/listings/{id}/files"
---

<objective>
Implement image and file upload to Etsy CDN.

Purpose: Handle image uploads to Etsy and digital product file delivery via Etsy. Per D-03: Upload to Etsy CDN. Per D-04: Product images from Phase 2 get uploaded. Per D-18: Etsy file hosting.

Output: Working image and file upload integrated with listing creation.
</objective>

<context>
@.planning/phases/03-listing-creation/03-RESEARCH.md (Pattern 2, Pattern 3 - image and file upload)
@.planning/phases/03-listing-creation/03-CONTEXT.md (D-03, D-04, D-18 locked decisions)
@.planning/phases/03-listing-creation/03-02-PLAN.md (Listing creation - must complete first)

# Reference
@src/etsy/client.py (EtsyClient from Plan 01)
</context>

<tasks>

<task type="auto">
<name>Task 1: Implement image upload to Etsy CDN</name>
  <files>src/etsy/image_upload.py</files>
  <action>
Create image upload module:

- `upload_listing_image(client, shop_id, listing_id, image_path) -> image_id`:
  - Upload single image to listing
  - Support: JPEG, PNG (Etsy requirements)
  - Returns: image_id from Etsy

- `upload_product_images(client, shop_id, listing_id, image_paths) -> list[image_id]`:
  - Upload multiple images (max 10 per Etsy limit)
  - First image = main listing image
  - Per D-03: Upload to Etsy CDN

- Helper: `convert_pdf_to_images(pdf_path)` (per D-04):
  - Use PyPDF2/pdf2image to convert Phase 2 PDF to images
  - Return list of image paths for upload
  - Square preferred, max 20 images (Etsy allows 10 per listing)

Per D-04: Product images from Phase 2 (PDF preview) get uploaded to Etsy.

Reference: 03-RESEARCH.md Pattern 2 (Create Draft Listing with Image Upload).
  </action>
  <verify>
<automated>python -c "from src.etsy.image_upload import upload_listing_image, upload_product_images; print('OK')"</automated>
  </verify>
  <done>Image upload module can upload images to Etsy listing</done>
</task>

<task type="auto">
<name>Task 2: Implement digital file upload</name>
  <files>src/etsy/file_upload.py</files>
  <action>
Create digital file upload module:

- `upload_digital_file(client, shop_id, listing_id, file_path) -> file_id`:
  - Upload PDF to Etsy
  - Per D-18: Etsy file hosting - Etsy handles download delivery
  - Returns: file_id from Etsy

- `set_listing_as_download(client, shop_id, listing_id)`:
  - After file upload, set type to "download"
  - Required for digital product delivery

- Per D-19: Set quantity to unlimited (Etsy handles this for digital)

Reference: 03-RESEARCH.md Pattern 3 (Digital Product File Upload).
  </action>
  <verify>
<automated>python -c "from src.etsy.file_upload import upload_digital_file, set_listing_as_download; print('OK')"</automated>
  </verify>
  <done>Digital file upload module can upload PDF to Etsy and set listing as download</done>
</task>

<task type="auto">
<name>Task 3: Add image/file endpoints to listing routes</name>
  <files>src/api/listing_routes.py</files>
  <action>
Add to existing listing_routes.py:

- `POST /api/listings/{listing_id}/images`:
  - Input: file (image)
  - Upload to Etsy, return image_id
  
- `POST /api/listings/{listing_id}/images/bulk`:
  - Input: list of image files
  - Upload multiple images (max 10)

- `POST /api/listings/{listing_id}/files`:
  - Input: file (PDF)
  - Upload digital file, set type to download
  - Per D-18: Etsy handles download delivery

- `GET /api/listings/{listing_id}/images`: List uploaded images
- `DELETE /api/listings/{listing_id}/images/{image_id}`: Remove image

Reference: 03-UI-SPEC.md endpoint list.
  </action>
  <verify>
<automated>python -c "from src.api.listing_routes import router; paths = [r.path for r in router.routes]; print('images' in str(paths), 'files' in str(paths))"</automated>
  </verify>
  <done>Image and file upload endpoints added to listing routes</done>
</task>

</tasks>

<verification>
- Images upload to Etsy and appear in listing
- PDF files upload to Etsy for digital delivery
- Etsy handles buyer download delivery
</verification>

<success_criteria>
User can upload images to listing (from Phase 2 PDF or direct upload), and digital products are delivered via Etsy file hosting.
</success_criteria>

<output>
After completion, create `.planning/phases/03-listing-creation/03-03-SUMMARY.md`
</output>