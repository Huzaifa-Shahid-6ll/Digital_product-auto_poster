---
phase: 04-research-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/niche_research/verifier.py
  - src/niche_research/__init__.py
  - src/niche_research/schemas.py
autonomous: true
requirements:
  - NR-02
user_setup:
  - service: google-trends
    why: "Market demand verification"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "System verifies niche recommendations with real market data from Google Trends"
    - "Verified niches include demand_score (0-100), trend_direction, and competition_factor"
    - "Scoring formula: search_interest * 0.4 + competition_factor * 0.3 + trend_direction * 0.3"
    - "Thresholds: 65+ = validated, 50-65 = explore, <50 = low demand"
  artifacts:
    - path: "src/niche_research/verifier.py"
      provides: "Google Trends verification and demand scoring"
      min_lines: 100
    - path: "src/niche_research/schemas.py"
      provides: "Updated with VerifiedNiche and demand scoring"
  key_links:
    - from: "src/niche_research/verifier.py"
      to: "src/niche_research/analyzer.py"
      via: "Input: list of NicheRecommendation"
      pattern: "verify_demand"
    - from: "src/niche_research/verifier.py"
      to: "src/niche_research/schemas.py"
      via: "Output: VerifiedNiche with demand_score"
      pattern: "VerifiedNiche"
---

<objective>
Verify niche recommendations with real market data using Google Trends API.

Purpose: Validate AI-generated recommendations with actual search interest data, calculate demand scores using the proven formula, and flag niches that don't meet minimum thresholds.

Output: VerifiedNiche objects with demand_score, trend_direction, verification_data, and source citations.
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md

# Dependencies
- Plan 01: NicheRecommendation schema and analyzer
- Must import and verify against output from 04-01
</execution_context>

<context>
@.planning/phases/04-research-validation/04-RESEARCH.md

# Demand scoring formula from research
- search_interest * 0.4 + competition_factor * 0.3 + trend_direction * 0.3
- search_interest: Google Trends average (0-100)
- trend_direction: +1 (rising), 0 (stable), -1 (declining)
- competition_factor: High=0.3, Medium=0.6, Low=1.0

# Thresholds
- 65+: Validated niche (only ~1% of niches)
- 50-65: Worth exploring
- <50: Low demand, not recommended

# pytrends usage (per research)
- Rate limit: 1 request per second
- Cache results for 15-minute TTL
- Handle 429 with exponential backoff + retry
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Create demand verification with Google Trends</name>
  <files>src/niche_research/verifier.py</files>
  <action>
    Create Google Trends verification system:

    1. Create `verify_demand(recommendations: list[NicheRecommendation]) -> list[VerifiedNiche]` function
    2. Use pytrends library to fetch search interest data:
       - Build payload with niche keywords
       - Fetch interest_over_time() for last 12 months
       - Calculate average_interest, trend_direction, peak_month
    3. Implement demand scoring formula:
       - search_interest * 0.4 + competition_factor * 0.3 + trend_direction * 0.3
       - competition_factor mapping: high=0.3, medium=0.6, low=1.0
    4. Apply thresholds: 65+ = validated, 50-65 = explore, <50 = low_demand
    5. Add rate limiting (1 req/sec) and retry logic (per D-04)
    6. Cache results with 15-minute TTL
    
    Key: Return VerifiedNiche with all verification_data for source citation
  </action>
  <verify>
    <automated>python -c "from src.niche_research.verifier import verify_demand; print('Verifier imports OK')"</automated>
  </verify>
  <done>verify_demand() returns list of VerifiedNiche with demand_score 0-100</done>
</task>

<task type="auto">
<parameter name>Task 2: Update schemas with VerifiedNiche model</name>
  <files>src/niche_research/schemas.py</files>
  <action>
    Update schemas to include verification models:

    1. Add `VerifiedNiche` model:
       - recommendation: NicheRecommendation (the original)
       - demand_score: int (0-100)
       - trend_direction: Literal["rising", "stable", "declining"]
       - verification_data: dict (raw Google Trends data)
       - verified_demand: bool (meets threshold of 50+)
       - user_approved: bool = False
    2. Add `VerificationResult` model for API response:
       - verified_niches: list[VerifiedNiche]
       - summary: dict with counts by category
       - verified_at: datetime
    
    Pattern: Extend existing NicheRecommendation, don't replace
  </action>
  <verify>
    <automated>python -c "from src.niche_research.schemas import VerifiedNiche, VerificationResult; print('Verification schemas OK')"</automated>
  </verify>
  <done>VerifiedNiche includes demand_score, trend_direction, verification_data</done>
</task>

<task type="auto">
<parameter name>Task 3: Create verification API endpoint</name>
  <files>src/api/research_routes.py</files>
  <action>
    Add verification endpoint to research routes:

    1. Add `POST /api/research/verify` endpoint
       - Input: list of NicheRecommendation (from Plan 01)
       - Calls verify_demand() with recommendations
       - Returns: VerificationResult with verified niches
    2. Add error handling for API failures (Google Trends rate limits)
    3. Return appropriate HTTP status codes

    Pattern: Follow same patterns as analyze endpoint
  </action>
  <verify>
    <automated>python -c "from src.api.research_routes import router; print('Router with verify OK')"</automated>
  </verify>
  <done>POST /api/research/verify returns verified niches with demand scores</done>
</task>

</tasks>

<verification>
- [ ] verify_demand() fetches real Google Trends data
- [ ] Each verified niche has demand_score (0-100)
- [ ] Trend direction correctly identified (rising/stable/declining)
- [ ] Thresholds applied correctly (65+ validated, 50-65 explore, <50 low)
- [ ] Source citations included (Google Trends URL)
</verification>

<success_criteria>
System verifies recommendations with real market data:
- demand_score calculated using formula: search_interest * 0.4 + competition_factor * 0.3 + trend_direction * 0.3
- Each niche has verified_demand flag based on 50+ threshold
- Raw verification_data stored for source citation
- API endpoint returns structured VerificationResult
</success_criteria>

<output>
After completion, create `.planning/phases/04-research-validation/04-02-SUMMARY.md`
</output>
