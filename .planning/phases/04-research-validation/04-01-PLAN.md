---
phase: 04-research-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/niche_research/__init__.py
  - src/niche_research/schemas.py
  - src/niche_research/analyzer.py
  - src/niche_research/sources.py
  - src/api/research_routes.py
  - src/db/models.py
autonomous: true
requirements:
  - NR-01
  - NR-02
user_setup:
  - service: openai
    why: "AI-powered market analysis"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API Keys"
    dashboard_config: []

must_haves:
  truths:
    - "User can input niche keywords and receive AI-generated niche recommendations with demand estimates"
    - "System returns structured output with target_audience, competition_level, and rationale for each recommendation"
    - "System includes source citations with each recommendation to prevent hallucinations"
  artifacts:
    - path: "src/niche_research/schemas.py"
      provides: "Pydantic models for NicheRecommendation"
      min_lines: 50
    - path: "src/niche_research/analyzer.py"
      provides: "AI-powered niche analysis with GPT-4o"
      min_lines: 80
    - path: "src/niche_research/sources.py"
      provides: "Source citation tracking and formatting"
      min_lines: 40
    - path: "src/api/research_routes.py"
      provides: "FastAPI endpoints for niche analysis"
      exports: ["POST /api/research/analyze"]
  key_links:
    - from: "src/niche_research/analyzer.py"
      to: "src/niche_research/schemas.py"
      via: "Pydantic validation"
      pattern: "NicheRecommendation"
    - from: "src/api/research_routes.py"
      to: "src/niche_research/analyzer.py"
      via: "FastAPI dependency injection"
      pattern: "analyze_niche"
---

<objective>
Create AI-powered niche analysis system that generates structured recommendations with source citations.

Purpose: Enable users to input niche keywords and receive 3+ AI-generated niche recommendations with demand estimates, target audiences, and rationale. Each recommendation must cite sources to prevent hallucinations.

Output: FastAPI endpoint accepting keywords, returning structured NicheRecommendation objects.
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md

# Patterns to reuse from prior phases
- src/product_generation/schemas.py: Pydantic model patterns
- src/workflows/base.py: BaseWorkflow ABC with StepDefinition
- src/core/state.py: TypedDict for state management
- Per D-04: 3 retries with exponential backoff (initial_interval=1.0, backoff_factor=2.0, max_attempts=3)
</execution_context>

<context>
@.planning/phases/04-research-validation/04-RESEARCH.md

# Key architecture from research
- Use OpenAI GPT-4o with structured output (json_object response format)
- Pydantic BaseModel for validation (per Phase 1, TypedDict for state, Pydantic for schemas)
- Source citation format: [Source: name](url)
- NicheRecommendation includes: niche, target_audience, demand_estimate, competition_level, recommended_formats, rationale, sources
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pydantic schemas for niche recommendations</name>
  <files>src/niche_research/schemas.py, src/niche_research/__init__.py</files>
  <action>
    Create Pydantic models for niche research structured output:

    1. Create `src/niche_research/__init__.py` - module exports
    2. Create `src/niche_research/schemas.py` with:
       - `NicheRecommendation`: niche, target_audience, demand_estimate (high/medium/low), competition_level (low/medium/high), recommended_formats, rationale, sources (list of source URLs)
       - `NicheAnalysisRequest`: keywords (list of str)
       - `NicheAnalysisResponse`: recommendations (list[NicheRecommendation]), analyzed_at (datetime)
    
    Reference: src/product_generation/schemas.py for Pydantic patterns
  </action>
  <verify>
    <automated>python -c "from src.niche_research.schemas import NicheRecommendation, NicheAnalysisRequest, NicheAnalysisResponse; print('Schemas import OK')"</automated>
  </verify>
  <done>NicheRecommendation includes all required fields, validation works</done>
</task>

<task type="auto">
  <name>Task 2: Create source citation tracking module</name>
  <files>src/niche_research/sources.py</files>
  <action>
    Create source citation system to prevent hallucinations:

    1. Create `Source` dataclass with url, name, accessed_at
    2. Create `SourceTracker` class that:
       - Tracks all sources used in analysis
       - Formats citations as `[Source: name](url)`
       - Provides method to inject sources into AI prompts
    3. Add helper to parse AI response and extract cited sources

    Pattern: Include sources in system prompt to ground AI responses
  </action>
  <verify>
    <automated>python -c "from src.niche_research.sources import Source, SourceTracker; tracker = SourceTracker(); print('SourceTracker OK')"</automated>
  </verify>
  <done>Source citation system tracks and formats sources correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create AI niche analyzer with OpenAI</name>
  <files>src/niche_research/analyzer.py</files>
  <action>
    Create AI-powered niche analyzer using GPT-4o:

    1. Create `analyze_niche(keywords: list[str]) -> list[NicheRecommendation]` function
    2. Use OpenAI `response_format={"type": "json_object"}` for structured output
    3. System prompt: "You are a market research analyst. Analyze niches and recommend profitable digital product options. Cite your sources using [Source: name](url) format."
    4. Parse AI response, validate with Pydantic, return structured recommendations
    5. Apply retry logic: 3 retries with exponential backoff (per D-04)
    
    Key: The AI generates initial recommendations WITHOUT verification data - that comes in Plan 02
  </action>
  <verify>
    <automated>python -c "from src.niche_research.analyzer import analyze_niche; print('Analyzer imports OK')"</automated>
  </verify>
  <done>analyze_niche() returns list of NicheRecommendation with demand estimates</done>
</task>

<task type="auto">
  <name>Task 4: Create FastAPI research endpoints</name>
  <files>src/api/research_routes.py</files>
  <action>
    Create FastAPI routes for niche research:

    1. Create `POST /api/research/analyze` endpoint
       - Input: {"keywords": ["keyword1", "keyword2"]}
       - Calls analyze_niche() with keywords
       - Returns: NicheAnalysisResponse with list of recommendations
    2. Add request validation using NicheAnalysisRequest
    3. Add error handling with appropriate HTTP status codes
    4. Register router at /api/research prefix

    Reference: src/api/idea_routes.py for API patterns
  </action>
  <verify>
    <automated>python -c "from src.api.research_routes import router; print('Router imports OK')"</automated>
  </verify>
  <done>POST /api/research/analyze returns structured recommendations</done>
</task>

</tasks>

<verification>
- [ ] NicheRecommendation Pydantic model validates correctly
- [ ] analyze_niche() returns 3+ recommendations per input keywords
- [ ] Each recommendation includes demand_estimate and competition_level
- [ ] Source citations present in output
- [ ] API endpoint responds with valid JSON
</verification>

<success_criteria>
User can input niche keywords and receive AI-generated niche recommendations with:
- demand_estimate (high/medium/low)
- competition_level (low/medium/high)
- target_audience
- rationale
- source citations

System returns structured output that can be parsed programmatically.
</success_criteria>

<output>
After completion, create `.planning/phases/04-research-validation/04-01-SUMMARY.md`
</output>
