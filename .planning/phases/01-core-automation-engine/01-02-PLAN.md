---
phase: 01-core-automation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - src/workflows/base.py
  - src/workflows/playbook.py
  - src/core/engine.py
  - src/core/checkpoint.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can define a playbook workflow with multiple steps"
    - "Workflow state persists across executions (save/resume)"
    - "Agent can route between workflow steps based on context"
  artifacts:
    - path: "src/workflows/base.py"
      provides: "BaseWorkflow abstract class for defining multi-step workflows"
      min_lines: 40
    - path: "src/workflows/playbook.py"
      provides: "PlaybookWorkflow implementing 10-step Digital Product Validation Playbook"
      min_lines: 60
    - path: "src/core/engine.py"
      provides: "WorkflowEngine class with LangGraph StateGraph, checkpointing, and execution"
      min_lines: 50
    - path: "src/core/checkpoint.py"
      provides: "SQLite checkpointer configuration for state persistence"
      min_lines: 20
  key_links:
    - from: "src/workflows/base.py"
      to: "src/core/engine.py"
      via: "BaseWorkflow is instantiated and run by WorkflowEngine"
      pattern: "class BaseWorkflow.*WorkflowEngine"
    - from: "src/core/engine.py"
      to: "src/core/checkpoint.py"
      via: "WorkflowEngine uses SqliteSaver for state persistence"
      pattern: "SqliteSaver.*checkpointer"
    - from: "src/core/state.py (from plan 01)"
      to: "src/core/engine.py"
      via: "Engine uses WorkflowState TypedDict for graph state"
      pattern: "StateGraph.*WorkflowState"
---

<objective>
Build workflow definition system and LangGraph engine with checkpoint persistence.

Purpose: Enable users to define multi-step workflows (like the 10-step playbook), execute them with state persistence, and route between steps based on context.
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-automation-engine/01-01-PLAN.md (foundation - state schema, db models)
@.planning/phases/01-core-automation-engine/01-RESEARCH.md (patterns 1-4 for LangGraph, checkpoint, retry)
@.planning/PROJECT.md (10-step playbook from VISION.md)

**Key interfaces from plan 01:**
- WorkflowState TypedDict in src/core/state.py
- WorkflowExecution, WorkflowStep models in src/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base workflow class</name>
  <files>src/workflows/base.py</files>
  <action>
    Create src/workflows/base.py with:
    - Abstract base class BaseWorkflow (ABC)
    - Properties: name, description, steps (list of StepDefinition)
    - StepDefinition dataclass with: name, handler_fn, retry_policy (optional), is_checkpoint (bool)
    - Abstract method get_graph() that returns compiled LangGraph StateGraph
    - Helper method add_step() to register steps
    - Abstract method get_initial_state() -> WorkflowState
    
    This establishes the contract for defining workflows.
  </action>
  <verify>
  <automated>python -c "from src.workflows.base import BaseWorkflow, StepDefinition; print('BaseWorkflow defined')"</automated>
  </verify>
  <done>BaseWorkflow abstract class exists with all required methods and properties</done>
</task>

<task type="auto">
  <name>Task 2: Create playbook workflow implementation</name>
<files>src/workflows/playbook.py</files>
<action>
    Create src/workflows/playbook.py implementing the 10-step Digital Product Validation Playbook:
    - Import BaseWorkflow from base.py
    - Import WorkflowState from src.core.state
    - Define PlaybookWorkflow class extending BaseWorkflow
    - Implement all 10 steps as handler functions:
      1. pick_niche - User inputs niche keywords
      2. check_demand - Verify market demand on Etsy
      3. decide_angle - Choose competitor to beat
      4. define_product - Plain language product description
      5. build_minimum - Create smallest version
      6. make_credible - Add professional styling
      7. set_price - Price for validation ($9-$19)
      8. create_listing - Generate Etsy listing
      9. get_traffic - Drive traffic to listing
      10. check_results - Analyze performance
    - Each step updates WorkflowState with step_results
    - Include conditional routing - some steps branch based on context
    
    Reference VISION.md for step details.
  </action>
  <verify>
  <automated>python -c "from src.workflows.playbook import PlaybookWorkflow; w = PlaybookWorkflow(); print(f'Workflow: {w.name}, Steps: {len(w.steps)}')"</automated>
  </verify>
  <done>PlaybookWorkflow exists with 10 steps, can be instantiated, get_graph() returns compiled LangGraph</done>
</task>

<task type="auto">
  <name>Task 3: Implement LangGraph engine with checkpointing</name>
<files>src/core/engine.py, src/core/checkpoint.py</files>
  <action>
    Create src/core/checkpoint.py:
    - Import SqliteSaver from langgraph.checkpoint.sqlite
    - Function create_checkpointer(db_path: str) -> SqliteSaver
    - Support for both in-memory (":memory:") and file-based SQLite
    
    Create src/core/engine.py:
    - Import StateGraph, START, END from langgraph.graph
    - Import WorkflowState from state.py
    - Import SqliteSaver from checkpoint.py
    - Class WorkflowEngine with:
      - __init__(workflow: BaseWorkflow, db_path: str = "workflows.db")
      - compile() method that builds the graph with checkpointing
      - run(initial_state: dict, thread_id: str) -> dict - executes workflow
      - get_state(thread_id: str) -> WorkflowState - retrieves current state
      - list_executions() -> list of execution records
    - Use conditional edges for step routing based on step_results
    - Handle interruption recovery via checkpoint resume
    
    Per D-01: Uses LangGraph for orchestration
    Per D-03: Uses SQLite for state persistence
  </action>
  <verify>
  <automated>python -c "
from src.workflows.playbook import PlaybookWorkflow
from src.core.engine import WorkflowEngine

wf = PlaybookWorkflow()
engine = WorkflowEngine(wf)
compiled = engine.compile()
print('WorkflowEngine compiled successfully')
"</automated>
  </verify>
  <done>WorkflowEngine can compile PlaybookWorkflow, execute with persistence, retrieve state</done>
</task>

<task type="auto">
  <name>Task 4: Implement conditional step routing</name>
<files>src/core/engine.py</files>
  <action>
    Add to src/core/engine.py:
    - Router function that examines WorkflowState.step_results to decide next step
    - Support for both linear (step N -> step N+1) and branching (step N -> step X or Y)
    - Example routing: After check_demand, if demand is low -> retry or skip to next niche
    - Default routing: proceed to next step in sequence
    - Add is_checkpoint flag to trigger human verification (per playbook step requirements)
    
    Pattern from RESEARCH.md: Conditional Edge Fallbacks (pattern 4)
  </action>
  <verify>
  <automated>python -c "
from src.workflows.playbook import PlaybookWorkflow
from src.core.engine import WorkflowEngine

wf = PlaybookWorkflow()
engine = WorkflowEngine(wf)
graph = engine.compile()
print('Graph has conditional edges:', 'conditional_edges' in str(graph))
"</automated>
  </verify>
  <done>Routing function implemented - workflow can branch based on context, not just linear</done>
</task>

</tasks>

<verification>
- [ ] BaseWorkflow abstract class defines workflow contract
- [ ] PlaybookWorkflow implements all 10 playbook steps
- [ ] WorkflowEngine compiles workflow with LangGraph
- [ ] Checkpoint persistence works - can resume from interruption
- [ ] Conditional routing allows branching between steps
</verification>

<success_criteria>
User can define playbook workflow with multiple steps, state persists across executions (save/resume works), agent can route between workflow steps based on context. Three success criteria addressed.
</success_criteria>

<output>
After completion, update `.planning/phases/01-core-automation-engine/phase-01-SUMMARY.md`
</output>