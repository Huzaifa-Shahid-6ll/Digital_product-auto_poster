---
phase: 01-core-automation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__init__.py
  - pyproject.toml
  - src/core/__init__.py
  - src/core/state.py
  - src/db/__init__.py
  - src/db/models.py
  - src/db/schema.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Project structure exists with LangGraph, FastAPI, SQLAlchemy, Typer, Rich dependencies"
    - "SQLite database schema defined for workflow execution persistence"
    - "State schema defined for LangGraph workflow state management"
  artifacts:
    - path: "src/core/state.py"
      provides: "TypedDict state schema with messages, current_step, step_results, errors"
      min_lines: 30
    - path: "src/db/models.py"
      provides: "SQLAlchemy models for workflow_execution and workflow_step tables"
      min_lines: 40
    - path: "pyproject.toml"
      provides: "Python dependencies: langgraph, fastapi, sqlalchemy, typer, rich"
      contains: "langgraph"
  key_links:
    - from: "src/core/state.py"
      to: "src/db/models.py"
      via: "State maps to database models for persistence"
      pattern: "class WorkflowState.*TypedDict"
---

<objective>
Set up foundational project structure, dependencies, database schema, and state definitions for the automation engine.

Purpose: Create the infrastructure layer that all downstream components depend on - no working features yet, just the skeleton that enables everything else.
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 1 success criteria)
@.planning/phases/01-core-automation-engine/01-CONTEXT.md (decisions D-01 to D-05)
@.planning/phases/01-core-automation-engine/01-RESEARCH.md (stack recommendations)

**Stack per D-01, D-03:**
- LangGraph 0.2.70+ for orchestration
- SQLite 3.x for persistence
- SQLAlchemy 2.0+ for ORM
- Typer 0.15+ for CLI
- Rich 13.x for terminal output
- FastAPI 0.115+ for API/dashboard
</context>

<interfaces>
<!-- Key interfaces this plan creates (consumed by later plans) -->

From src/core/state.py:
```python
class WorkflowState(TypedDict):
    """State flows through every node in the graph."""
    messages: Annotated[list[BaseMessage], add_messages]
    current_step: str
    step_results: dict
    errors: list[str]
    metadata: dict
```

From src/db/models.py:
```python
class WorkflowExecution(Base):
    """Stores workflow execution state for persistence."""
    id: str  # thread_id
    workflow_name: str
    status: ExecutionStatus
    current_step: str
    state_data: dict
    error_message: str | None

class WorkflowStep(Base):
    """Stores individual step execution history."""
    id: int
    execution_id: str
    step_name: str
    status: str
    input_data: dict
    output_data: dict
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project structure and dependencies</name>
  <files>pyproject.toml</files>
  <action>
    Create pyproject.toml with all required dependencies:
    - langgraph>=0.2.70 (orchestration)
    - fastapi>=0.115 (API/dashboard)
    - uvicorn[standard]>=0.30 (ASGI server)
    - sqlalchemy>=2.0 (ORM)
    - aiosqlite (async SQLite support)
    - typer>=0.15 (CLI framework)
    - rich>=13.0 (terminal output)
    - pydantic>=2.7 (validation)
    - pydantic-settings>=2.1 (config)
    - python-dotenv (env vars)
    - httpx (async HTTP client)
    - pytest (testing)
    
    Include project metadata: name, version, description.
  </action>
  <verify>
    <automated>python -c "import langgraph; import fastapi; import sqlalchemy; import typer; import rich; print('All dependencies available')"</automated>
  </verify>
  <done>pyproject.toml exists with all dependencies listed under [project.dependencies]</done>
</task>

<task type="auto">
  <name>Task 2: Create project directory structure</name>
  <files>src/__init__.py, src/core/__init__.py, src/db/__init__.py, src/workflows/__init__.py, src/api/__init__.py, src/cli/__init__.py</files>
  <action>
    Create directory structure per RESEARCH.md architecture:
    - src/core/ (engine, state, checkpoint)
    - src/workflows/ (base workflow, playbook)
    - src/api/ (FastAPI main, routes, models)
    - src/cli/ (commands, output)
    - src/db/ (models, schema)
    - src/utils/ (logging, errors)
    
    Create __init__.py files in each directory for Python package recognition.
  </action>
  <verify>
    <automated>ls -la src/ && ls -la src/core/ && ls -la src/db/</automated>
  </verify>
  <done>All directories exist with __init__.py files</done>
</task>

<task type="auto">
  <name>Task 3: Define LangGraph state schema</name>
  <files>src/core/state.py</files>
  <action>
    Create src/core/state.py with:
    - Import TypedDict, Annotated from typing
    - Import add_messages from langgraph.graph
    - Import BaseMessage from langchain.schema
    - Define WorkflowState TypedDict with:
      - messages: Annotated[list[BaseMessage], add_messages] - appends to history
      - current_step: str - current step name
      - step_results: dict - results from each step
      - errors: list[str] - error messages
      - metadata: dict - additional context
    - Include type hints throughout
  </action>
  <verify>
    <automated>python -c "from src.core.state import WorkflowState; print('WorkflowState defined:', WorkflowState)"</automated>
  </verify>
  <done>WorkflowState TypedDict exists with all required fields per pattern in RESEARCH.md</done>
</task>

<task type="auto">
  <name>Task 4: Create SQLite database models</name>
  <files>src/db/models.py, src/db/schema.py</files>
  <action>
    Create src/db/models.py with SQLAlchemy models:
    - Create enum ExecutionStatus (PENDING, RUNNING, COMPLETED, FAILED)
    - Create WorkflowExecution model with columns:
      - id (String, primary_key=True) - thread_id
      - workflow_name (String)
      - status (Enum ExecutionStatus)
      - current_step (String)
      - state_data (JSON) - serialized LangGraph state
      - error_message (String, nullable)
      - created_at (DateTime)
      - updated_at (DateTime)
    - Create WorkflowStep model with columns:
      - id (Integer, autoincrement)
      - execution_id (String, foreign key)
      - step_name (String)
      - status (String) - started, completed, failed
      - input_data (JSON)
      - output_data (JSON)
      - error (String, nullable)
      - started_at (DateTime)
      - completed_at (DateTime, nullable)
    
    Create src/db/schema.py that creates the database engine and tables.
    Use SQLAlchemy 2.0 async style but with sync for MVP simplicity.
  </action>
  <verify>
    <automated>python -c "from src.db.models import WorkflowExecution, WorkflowStep; from src.db.schema import init_db; init_db(); print('Database models created')"</automated>
  </verify>
  <done>SQLAlchemy models exist matching RESEARCH.md schema, init_db() creates SQLite file</done>
</task>

</tasks>

<verification>
- [ ] pyproject.toml contains all required dependencies
- [ ] Directory structure matches RESEARCH.md architecture
- [ ] WorkflowState TypedDict defined with add_messages reducer
- [ ] WorkflowExecution and WorkflowStep models created
- [ ] init_db() successfully creates SQLite database
</verification>

<success_criteria>
Project has foundational structure: dependencies installed, directories created, state schema defined, database models ready. All artifacts are infrastructure - no working features yet.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-automation-engine/phase-01-SUMMARY.md`
</output>