---
phase: 01-core-automation-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02-PLAN.md
files_modified:
  - src/core/engine.py
  - src/utils/errors.py
  - src/cli/commands.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "System handles errors gracefully with retry logic"
  artifacts:
    - path: "src/utils/errors.py"
      provides: "Custom error classes: WorkflowError, StepError, RetryExhaustedError"
      min_lines: 25
    - path: "src/core/engine.py"
      provides: "RetryPolicy integration with exponential backoff, error handling nodes"
      min_lines: 30
    - path: "src/cli/commands.py"
      provides: "CLI commands: run, status, retry, list with error display"
      min_lines: 40
  key_links:
    - from: "src/core/engine.py"
      to: "src/utils/errors.py"
      via: "Engine catches and classifies errors, maps to RetryExhaustedError"
      pattern: "except.*WorkflowError"
    - from: "src/cli/commands.py"
      to: "src/core/engine.py"
      via: "CLI invokes engine.run() and displays error information"
      pattern: "engine.run.*error"
---

<objective>
Implement error handling with retry logic - the 4th success criterion.

Purpose: Ensure the automation engine handles failures gracefully with automatic retries (3 attempts, exponential backoff) and clear error reporting to users.
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-automation-engine/01-CONTEXT.md (D-04: Retry with fallback strategy - 3 retries, exponential backoff)
@.planning/phases/01-core-automation-engine/01-RESEARCH.md (pattern 3: RetryPolicies, pattern 4: Error handling nodes)

**Decision D-04 (locked):**
- 3 retries with exponential backoff
- Then fail with clear error message
- Log failures for debugging
</context>

<interfaces>
<!-- Interfaces from prior plans -->
From src/core/state.py:
```python
class WorkflowState(TypedDict):
    messages: Annotated[list[BaseMessage], add_messages]
    current_step: str
    step_results: dict
    errors: list[str]  # Error messages accumulated
    metadata: dict
```

From src.workflows.base.py:
```python
class BaseWorkflow(ABC):
    name: str
    steps: list[StepDefinition]
    def get_graph(self) -> CompiledStateGraph: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create custom error classes</name>
<files>src/utils/errors.py</files>
<action>
    Create src/utils/errors.py with:
    - WorkflowError - base exception for all workflow errors
    - StepError - raised when a specific step fails (includes step_name)
    - RetryExhaustedError - raised after all retries exhausted (includes last_error, attempts)
    - TransientError - marker for errors that should be retried (network, timeout)
    - PermanentError - marker for errors that should not be retried (bad input, auth)
    - Error classification helper: classify_error(error: Exception) -> ErrorType
    - Each error includes context: step_name, timestamp, original_error
    
    Per D-04: Clear error messages with failure context
  </action>
  <verify>
  <automated>python -c "
from src.utils.errors import WorkflowError, StepError, RetryExhaustedError, TransientError, PermanentError
e = StepError('check_demand', 'API timeout')
print(f'Error created: {e.step_name}, {e}')
"</automated>
  </verify>
  <done>All error classes defined with proper inheritance and context attributes</done>
</task>

<task type="auto">
  <name>Task 2: Integrate RetryPolicy into workflow engine</name>
<files>src/core/engine.py</files>
  <action>
    Update src/core/engine.py to add retry logic:
    - Import RetryPolicy from langgraph.types
    - Define default retry policy: initial_interval=1.0, backoff_factor=2.0, max_interval=30.0, max_attempts=3, jitter=True
    - Attach retry_policy to each step node in the graph
    - Add error_handler node that catches failures, logs to errors list in state
    - Add conditional edge: if errors exist -> error_handler, else continue
    
    Per D-04: 3 retries with exponential backoff
    Pattern from RESEARCH.md: RetryPolicy (pattern 3)
  </action>
  <verify>
  <automated>python -c "
from langgraph.types import RetryPolicy
from src.core.engine import WorkflowEngine
from src.workflows.playbook import PlaybookWorkflow

policy = RetryPolicy(initial_interval=1.0, backoff_factor=2.0, max_attempts=3, jitter=True)
print('RetryPolicy configured:', policy.max_attempts, 'attempts')
"</automated>
  </verify>
  <done>WorkflowEngine applies RetryPolicy to steps, handles failures gracefully</done>
</task>

<task type="auto">
  <name>Task 3: Add error handling node and state management</name>
<files>src/core/engine.py</files>
<action>
    Update src/core/engine.py:
    - Add error_handler(state: WorkflowState) -> WorkflowState:
      - Receives current step that failed
      - Adds error message to state.errors
      - Determines if should retry, skip step, or abort
      - Logs failure for debugging
    - Update step handler to catch exceptions and route to error_handler
    - If retries exhausted (per D-04), raise RetryExhaustedError with clear message
    - State.errors accumulates all errors across workflow execution
    
    Per D-04: Clear error message after retries exhausted
  </action>
  <verify>
  <automated>python -c "
from src.core.state import WorkflowState
state = WorkflowState(messages=[], current_step='step_1', step_results={}, errors=[], metadata={})
state['errors'].append('Step check_demand failed: API timeout after 3 retries')
print('Error in state:', state['errors'])
"</automated>
  </verify>
  <done>Error handler node integrates with LangGraph, errors tracked in state</done>
</task>

<task type="auto">
  <name>Task 4: Add CLI commands for error visibility</name>
<files>src/cli/commands.py</files>
<action>
    Create src/cli/commands.py with Typer CLI commands:
    - run <workflow_name> - Execute a workflow, show progress with Rich
    - status <execution_id> - Show execution status including errors
    - list - Show all workflow executions with status
    - retry <execution_id> - Retry a failed workflow from last step
    - logs <execution_id> - Show error logs for a failed execution
    
    Use Rich for beautiful terminal output:
    - Progress spinner during execution
    - Error messages in red with step context
    - Table for listing executions
    
    Per D-05: CLI first approach
  </action>
  <verify>
  <automated>python -c "
import typer
from src.cli.commands import app
print('CLI app created:', app)
"</automated>
  </verify>
  <done>CLI commands exist and can be invoked, error information visible to users</done>
</task>

</tasks>

<verification>
- [ ] Error classes defined in src/utils/errors.py
- [ ] RetryPolicy with exponential backoff integrated into engine
- [ ] Error handler node catches failures, logs to state.errors
- [ ] Clear error messages after retries exhausted
- [ ] CLI displays errors to users
</verification>

<success_criteria>
System handles errors gracefully with retry logic - all 4 success criteria from ROADMAP.md now addressed.
</success_criteria>

<output>
After completion, update `.planning/phases/01-core-automation-engine/phase-01-SUMMARY.md`
</output>