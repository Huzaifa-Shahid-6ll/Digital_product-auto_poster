---
phase: 01-core-automation-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03-PLAN.md
files_modified:
  - src/api/main.py
  - src/api/routes.py
  - src/api/models.py
  - src/cli/__init__.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can monitor workflow execution via web dashboard"
  artifacts:
    - path: "src/api/main.py"
      provides: "FastAPI application with WebSocket for real-time updates"
      min_lines: 30
    - path: "src/api/routes.py"
      provides: "REST endpoints: POST /workflows, GET /executions/{id}, GET /executions"
      min_lines: 25
    - path: "src/api/models.py"
      provides: "Pydantic models for API request/response"
      min_lines: 20
  key_links:
    - from: "src/api/main.py"
      to: "src/core/engine.py"
      via: "API calls WorkflowEngine for execution and state"
      pattern: "WorkflowEngine.*run"
    - from: "src/api/main.py"
      to: "src/db/models.py"
      via: "API retrieves execution history from database"
      pattern: "WorkflowExecution.*query"
---

<objective>
Build minimal web dashboard for workflow monitoring.

Purpose: Per D-05, add minimal dashboard for workflow monitoring and visualization - status endpoints and execution history, no complex charts yet.
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-automation-engine/01-CONTEXT.md (D-05: CLI first + Web Dashboard)
@.planning/phases/01-core-automation-engine/01-RESEARCH.md (pattern 6: FastAPI + WebSocket dashboard)

**User's discretion:**
- Dashboard feature scope and complexity (minimal for MVP)
</context>

<interfaces>
<!-- Interfaces from prior plans -->
From src.core.engine.py:
```python
class WorkflowEngine:
    def run(self, initial_state: dict, thread_id: str) -> dict: ...
    def get_state(self, thread_id: str) -> WorkflowState: ...
    def list_executions(self) -> list[dict]: ...
```

From src.db.models.py:
```python
class WorkflowExecution:
    id: str
    workflow_name: str
    status: ExecutionStatus
    current_step: str
    error_message: str | None
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create FastAPI application and models</name>
<files>src/api/main.py, src/api/models.py</files>
<action>
    Create src/api/models.py with Pydantic models:
    - WorkflowStartRequest (workflow_name, initial_inputs)
    - WorkflowStartResponse (execution_id, status)
    - ExecutionStatusResponse (id, status, current_step, errors, created_at)
    - ExecutionListResponse (executions: list[ExecutionStatusResponse])
    
    Create src/api/main.py:
    - Import FastAPI, WebSocket
    - Import CORS middleware for frontend
    - Create app = FastAPI(title="Digital Product Auto-Poster API")
    - Add CORS: allow all origins for MVP development
    - Add health check endpoint: GET /health
    - Include WebSocket endpoint for real-time updates
    
    Per RESEARCH.md pattern 6: FastAPI + WebSocket
  </action>
  <verify>
  <automated>python -c "from src.api.main import app; from src.api.models import WorkflowStartRequest; print('FastAPI app created')"</automated>
  </verify>
  <done>FastAPI app initialized with Pydantic models and CORS</done>
</task>

<task type="auto">
  <name>Task 2: Create API routes</name>
<files>src/api/routes.py</files>
<action>
    Create src/api/routes.py:
    - POST /workflows - Start a workflow execution
      - Accept workflow_name, initial_inputs
      - Generate execution_id (thread_id)
      - Call engine.run() asynchronously
      - Return execution_id and initial status
    - GET /executions/{execution_id} - Get execution status
      - Retrieve from database or checkpoint
      - Return current step, status, errors
    - GET /executions - List all executions
      - Pagination: limit, offset
      - Filter by status (running, completed, failed)
    - GET /executions/{execution_id}/stream - WebSocket for real-time updates
      - Stream workflow state every 1 second
      - Close on completion or error
    
    Per D-05: Minimal dashboard - status + history only
  </action>
  <verify>
  <automated>python -c "
from src.api.routes import router
print('Routes defined:', [r.path for r in router.routes])
"</automated>
  </verify>
  <done>API routes exist for starting, listing, and monitoring workflows</done>
</task>

<task type="auto">
<name>Task 3: Wire API to engine</name>
<files>src/api/main.py</files>
<action>
    Update src/api/main.py to:
    - Import WorkflowEngine from src.core.engine
    - Import PlaybookWorkflow from src.workflows.playbook
    - Create engine instance at module level
    - Register routes with app
    - Add startup event to initialize database
    - Add exception handlers for workflow errors
    
    Make the API functional - can start and monitor workflows.
  </action>
  <verify>
  <automated>python -c "
from src.api.main import app
from src.core.engine import WorkflowEngine
from src.workflows.playbook import PlaybookWorkflow

wf = PlaybookWorkflow()
engine = WorkflowEngine(wf)
print('Engine wired to API')
"</automated>
  </verify>
  <done>API endpoints functional - can start workflows via POST and check status via GET</done>
</task>

<task type="auto">
  <name>Task 4: Create CLI entry point</name>
<files>src/cli/__init__.py</files>
<action>
    Update src/cli/__init__.py to:
    - Import app from commands.py (Typer)
    - Add main entry point for CLI
    - Define __main__ block so CLI can run: python -m src.cli
    - Register --db-path option for database location
    
    Per D-05: CLI first - primary interface for MVP
  </action>
  <verify>
  <automated>python -c "from src.cli import app; print('CLI ready')"</automated>
  </verify>
  <done>CLI can be invoked: python -m src.cli --help</done>
</task>

</tasks>

<verification>
- [ ] FastAPI app with Pydantic models created
- [ ] REST routes for workflow start, status, list
- [ ] WebSocket endpoint for real-time updates
- [ ] CLI entry point functional
- [ ] API wired to workflow engine
</verification>

<success_criteria>
Web dashboard provides workflow monitoring - users can start workflows via CLI or API and monitor via dashboard. Core automation engine complete with all 4 success criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-automation-engine/phase-01-SUMMARY.md`
</output>