---
phase: 05-analytics-measurement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [AN-01]
user_setup: []

must_haves:
  truths:
    - "User can view dashboard showing listing views, favorites, and sales"
    - "System attributes sales to specific listings and time periods"
  artifacts:
    - path: "src/analytics/__init__.py"
      provides: "Analytics module exports"
    - path: "src/analytics/schemas.py"
      provides: "Pydantic models for analytics data"
    - path: "src/analytics/collector.py"
      provides: "Etsy analytics data collection"
    - path: "src/analytics/aggregator.py"
      provides: "Metrics computation (views, favorites, sales)"
    - path: "src/db/schema.py"
      provides: "Analytics table definitions"
  key_links:
    - from: "src/analytics/collector.py"
      to: "src/etsy/client.py"
      via: "Etsy API calls"
    - from: "src/analytics/aggregator.py"
      to: "src/db/schema.py"
      via: "SQL queries"
---

<objective>
Create analytics backend: schemas, data collector, and metrics aggregator
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-analytics-measurement/05-RESEARCH.md

# From earlier phases - key patterns to follow:
# - Pydantic schemas from src/product_generation/schemas.py
# - Retry logic (exponential backoff) from earlier phases
# - Database models from src/db/schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics Pydantic schemas</name>
  <files>src/analytics/__init__.py, src/analytics/schemas.py</files>
  <read_first>
- src/product_generation/schemas.py (Pydantic patterns)
- src/db/models.py (existing database models)
  </read_first>
  <action>
Create analytics module with Pydantic models:

1. Create src/analytics/__init__.py with module exports
2. Create src/analytics/schemas.py with:
   - ListingMetrics: views, favorites, sales, date range
   - AnalyticsEvent: listing_id, event_type, timestamp, metadata
   - TimeSeriesData: date, value, metric_type
   - AttributionRecord: listing_id, sale_id, timestamp, source

Use Pydantic patterns from product_generation/schemas.py (BaseModel, Field with descriptions).
  </action>
  <verify>
    <automated>python -c "from src.analytics.schemas import ListingMetrics, AnalyticsEvent, AttributionRecord; print('schemas import OK')"</automated>
  </verify>
  <done>Pydantic schemas import successfully, all fields validated</done>
</task>

<task type="auto">
  <name>Task 2: Analytics data collector</name>
  <files>src/analytics/collector.py</files>
  <read_first>
- src/etsy/client.py (Etsy API patterns)
- src/analytics/schemas.py (output types)
  </read_first>
  <action>
Create analytics data collector:

1. Create src/analytics/collector.py
2. Implement collect_listing_stats(listing_id: str) -> ListingMetrics:
   - Fetch from Etsy API or database
   - Handle API errors with retry (exponential backoff)
3. Implement collect_analytics_events(listing_id: str, start_date: datetime, end_date: datetime) -> List[AnalyticsEvent]
4. Store events in SQLite database

Reference: Use retry pattern from earlier phases (3 retries, exponential backoff per D-04).
  </action>
  <verify>
    <automated>python -c "from src.analytics.collector import collect_listing_stats; print('collector import OK')"</automated>
  </verify>
  <done>Collector imports and connects to data source</done>
</task>

<task type="auto">
  <name>Task 3: Metrics aggregator</name>
  <files>src/analytics/aggregator.py</files>
  <read_first>
- src/analytics/schemas.py
- src/db/schema.py
  </read_first>
  <action>
Create metrics aggregator:

1. Create src/analytics/aggregator.py
2. Implement aggregate_listing_metrics(listing_id: str, period: str) -> ListingMetrics:
   - Aggregate views, favorites, sales by period (day/week/month)
3. Implement aggregate_time_series(listing_ids: List[str], metric: str, start_date: datetime, end_date: datetime) -> List[TimeSeriesData]
4. Implement compute_attribution(sale_id: str) -> AttributionRecord:
   - Link sale to listing and time period

Use SQLAlchemy patterns from existing db/models.py.
  </action>
  <verify>
    <automated>python -c "from src.analytics.aggregator import aggregate_listing_metrics; print('aggregator import OK')"</automated>
  </verify>
  <done>Aggregator computes metrics correctly</done>
</task>

</tasks>

<verification>
- [ ] Analytics module imports without errors
- [ ] Pydantic schemas validate input correctly
- [ ] Collector fetches data (mock or real)
- [ ] Aggregator computes metrics
</verification>

<success_criteria>
Backend components created: schemas, collector, aggregator. Ready for dashboard integration.
</success_criteria>

<output>
After completion, create `.planning/phases/05-analytics-measurement/05-01-SUMMARY.md`
</output>