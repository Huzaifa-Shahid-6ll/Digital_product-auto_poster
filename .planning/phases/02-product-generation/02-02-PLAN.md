---
phase: 02-product-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PG-02
user_setup: []

must_haves:
  truths:
    - System can auto-generate a PDF product (planner/template/guide) from a selected idea
    - Generated PDF passes quality validation (formatting, completeness, coherence)
    - PDF uses consistent formatting with proper structure
  artifacts:
    - path: src/product_generation/generator.py
      provides: PDF generation from product ideas
      min_lines: 60
    - path: src/product_generation/validator.py
      provides: Quality validation for generated PDFs
      exports: validate_completeness, validate_formatting, validate_coherence
    - path: src/api/product_routes.py
      provides: API endpoints for product generation
      exports: POST /api/products/generate
  key_links:
    - from: src/product_generation/generator.py
      to: src/product_generation/schemas.py
      via: ProductIdea input
      pattern: def generate(product_idea: ProductIdea)
    - from: src/product_generation/validator.py
      to: src/product_generation/generator.py
      via: validates output
      pattern: validate(product: ProductOutput)
    - from: src/api/product_routes.py
      to: src/product_generation/generator.py
      via: Generator dependency
      pattern: ProductGenerator dependency
---

<objective>
Auto-generate a PDF product (planner, worksheet, or guide) based on a selected product idea. PDF must pass quality validation before presenting to user.
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-product-generation/02-CONTEXT.md
@.planning/phases/02-product-generation/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-product-generation/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProductOutput schema and generator logic</name>
  <files>src/product_generation/generator.py</files>
  <read_first>
src/product_generation/schemas.py
src/product_generation/ideas.py
.planning/phases/02-product-generation/02-RESEARCH.md (Code Examples - PDF Generation)
</read_first>
  <action>
Add to schemas.py:
- ProductOutput: idea_id (str), format (str), title (str), content (dict with sections), generated_at (datetime), quality_score (float | None)

Create ProductGenerator class:
- __init__(client: OpenAI) - generates additional content using AI for the selected idea
- generate(product_idea: ProductIdea) -> ProductOutput - uses ReportLab to create PDF
- Supports formats: planner, worksheet, guide (per D-02: PDF only for MVP)
- Uses Jinja2 for content templating, ReportLab SimpleDocTemplate for PDF
- Adds page numbers, consistent styling, proper heading hierarchy

Output: bytes (PDF file content) stored in ProductOutput or saved to disk.
  </action>
  <verify>
<automated>grep -n "class ProductGenerator" src/product_generation/generator.py && grep -n "def generate" src/product_generation/generator.py</automated>
  </verify>
  <done>ProductGenerator creates PDF from ProductIdea with consistent formatting</done>
</task>

<task type="auto">
  <name>Task 2: Implement quality validation</name>
  <files>src/product_generation/validator.py</files>
  <read_first>
src/product_generation/generator.py
</read_first>
  <action>
Create ProductValidator class implementing D-03 (AI + Human review):

validate(product: ProductOutput) -> ValidationResult with:
- validate_completeness: checks all sections present, no empty content
- validate_formatting: checks consistent font sizes, proper spacing, no broken layout
- validate_coherence: uses AI to check content flows logically, no contradictions
- get_quality_score: returns 0-100 based on above checks

ValidationResult includes:
- passed: bool
- issues: list[str] (if any)
- score: float (0-100)
- recommendation: "approve" | "needs_revision" | "regenerate"

Per D-03: AI does initial assessment, human does final approval via dashboard.
  </action>
  <verify>
<automated>grep -n "class ProductValidator" src/product_generation/validator.py && grep -n "def validate" src/product_generation/validator.py</automated>
  </done>
  <done>Quality validator checks completeness, formatting, coherence per D-03</done>
</task>

<task type="auto">
  <name>Task 3: Create API endpoint for product generation</name>
  <files>src/api/product_routes.py</files>
  <read_first>
src/api/routes.py (existing patterns)
src/api/idea_routes.py (from Plan 01)
</read_first>
  <action>
Create FastAPI router for product generation:
- POST /api/products/generate - accepts {idea_id: str, format?: str}, returns ProductOutput + validation
- POST /api/products/validate - accepts ProductOutput, returns ValidationResult
- GET /api/products/{product_id} - retrieves generated product

Uses dependency injection for ProductGenerator and ProductValidator.
Saves generated PDF to disk, returns path in response.
  </action>
  <verify>
<automated>grep -n "router = APIRouter" src/api/product_routes.py && grep -n "@router.post" src/api/product_routes.py</automated>
  </verify>
  <done>API endpoints for generating and validating products accessible</done>
</task>

</tasks>

<verification>
- [ ] ProductGenerator creates PDF from ProductIdea
- [ ] PDF has consistent formatting (page numbers, fonts, spacing)
- [ ] Validator checks completeness, formatting, coherence
- [ ] Validation score returned before human review
- [ ] API endpoints handle generation and validation
</verification>

<success_criteria>
System auto-generates a digital product (PDF) based on selected idea. Generated product passes quality validation before user review.
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-generation/02-02-SUMMARY.md`
</output>