---
phase: 02-product-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PG-01
user_setup: []

must_haves:
  truths:
    - User can input a niche and receive 3+ product ideas with rationale
    - Ideas include format type, target audience, unique angle, and key features
    - System generates ideas using hybrid template + AI approach per D-01
  artifacts:
    - path: src/product_generation/ideas.py
      provides: Idea generation logic with structured output
      min_lines: 50
    - path: src/product_generation/schemas.py
      provides: Pydantic models for product ideas
      exports: ProductIdea, IdeaSet
    - path: src/api/idea_routes.py
      provides: REST endpoints for idea generation
      exports: POST /api/ideas/generate
  key_links:
    - from: src/product_generation/ideas.py
      to: OpenAI API
      via: openai client with structured output
      pattern: response_format.*json_object.*ProductIdea
    - from: src/api/idea_routes.py
      to: src/product_generation/ideas.py
      via: FastAPI dependency injection
      pattern: IdeaGenerator dependency
---

<objective>
Generate 3+ product ideas with rationale based on niche input. Each idea includes title, format type, target audience, unique angle, key features, and rationale for why it fits the niche.
</objective>

<execution_context>
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/workflows/execute-plan.md
@G:/ONGOING PROJECTS/Digital_product-auto_poster/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-product-generation/02-CONTEXT.md
@.planning/phases/02-product-generation/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-automation-engine/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProductIdea schemas</name>
  <files>src/product_generation/schemas.py</files>
  <action>
Create Pydantic models for structured AI output. Import from .planning/phases/01-core-automation-engine/01-CONTEXT.md that pydantic is already in use.

Define:
- ProductIdea: title (str), format (Literal["planner", "worksheet", "guide"]), target_audience (str), unique_angle (str), key_features (list[str]), rationale (str)
- IdeaSet: ideas (list[ProductIdea]), niche (str), generated_at (datetime)

Export both models for use by idea generation and API routes.
  </action>
  <verify>
<automated>python -c "from src.product_generation.schemas import ProductIdea, IdeaSet; print('Import OK')"</automated>
  </verify>
  <done>Pydantic models importable, validates ProductIdea with all required fields</done>
</task>

<task type="auto">
  <name>Task 2: Implement IdeaGenerator with OpenAI structured output</name>
  <files>src/product_generation/ideas.py</files>
  <read_first>
.src/product_generation/schemas.py
.planning/phases/02-product-generation/02-RESEARCH.md (Code Examples section)
</read_first>
  <action>
Create IdeaGenerator class:
- __init__(client: OpenAI) - stores OpenAI client
- generate(niche: str, count: int = 3) -> IdeaSet - calls GPT-4o with structured output
- System prompt instructs AI to generate diverse format types (planner, worksheet, guide)
- Each idea includes rationale explaining why it fits the niche
- Use response_format={"type": "json_object"} for structured output

Handle errors gracefully with retry logic (per D-04 from Phase 1: 3 retries with exponential backoff).
  </action>
  <verify>
<automated>grep -n "class IdeaGenerator" src/product_generation/ideas.py && grep -n "async def generate" src/product_generation/ideas.py</automated>
  </verify>
  <done>IdeaGenerator generates 3+ ideas per D-01 (hybrid template + AI approach)</done>
</task>

<task type="auto">
  <name>Task 3: Create API endpoint for idea generation</name>
  <files>src/api/idea_routes.py</files>
  <read_first>
src/api/routes.py (existing API patterns from Phase 1)
</read_first>
  <action>
Create FastAPI router for idea generation:
- POST /api/ideas/generate - accepts {niche: str, count?: int (default 3)}, returns IdeaSet
- Uses dependency injection for IdeaGenerator
- Validates niche is not empty, min 3 characters
- Returns 422 if validation fails, 200 on success

Mount router at /api/ideas in main app.
  </action>
  <verify>
<automated>grep -n "router = APIRouter" src/api/idea_routes.py && grep -n "@router.post" src/api/idea_routes.py</automated>
  </verify>
  <done>API endpoint accessible, accepts niche input, returns structured ideas</done>
</task>

</tasks>

<verification>
- [ ] ProductIdea schema validates all required fields
- [ ] IdeaGenerator calls OpenAI with structured output
- [ ] API endpoint accepts niche and returns 3+ ideas
- [ ] Each idea includes rationale explaining niche fit
- [ ] Error handling with retries per D-04 (Phase 1)
</verification>

<success_criteria>
User can input a niche and receive 3+ product ideas with rationale. Ideas include format, audience, angle, features, and rationale.
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-generation/02-01-SUMMARY.md`
</output>