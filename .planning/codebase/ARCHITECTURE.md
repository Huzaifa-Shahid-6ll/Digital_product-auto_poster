# Architecture

**Pattern:** Documentation-only project (no code architecture)

## Structure

The project follows a simple documentation structure:

- `VISION.md` - Main business methodology document
- `.planning/` - GSD workflow configuration directory
  - `config.json` - Configuration settings
  - `codebase/` - Codebase mapping documents (auto-generated)

## Data Flow

This is a static documentation project. No data flow, no processing, no entry points.

## Key Documents

- `VISION.md` - Digital Product Validation Playbook (human-voice guide)
